//var assert = require ('assert');
//var numbers = require('../Numbers');
//var Multiples = require ('../Multiples');



//The following test would have a description of a failed test.
//It would have a description of how the multiples of three Integers
// convert to the string "Fizz" and the same for 5


/*describe('Multiples', function () {
    describe('the constructor', function () {

        it('should throw if Multiple of 3 is not passed as Fizz', function () {
            assert.throws(() => new Multiples('bad'));
        });

    });

    describe('Multiples of 3', function () {

        let conversions = [
            {int: 3, str: "Fizz"},
            {int: 6, str: "Fizz"},
            //really this could be a loop ...?
        ];

        conversions.forEach(function (i) {
            it(`should be "${i.str}" when passed ${i.int}`, function () {
                let k = new Multiples(i.int);
                assert.equal(k.Multiples_of_3, i.str);
            });
        });


    });

    describe('The full numbers', function () {


        numbers.forEach(function (i) {
            it(`should be "${i.str}" when passed ${i.int}`, function () {
                let k = new Roman(i.int);
                assert.equal(k.full_numbers, i.str);
            });
        });
    })

});  */

